# Test-scoped logging: silence the expected WARN/exception output produced by
# the deliberate failure-path tests (subscriber-throws, file-write-failure,
# buffer-full). Tests assert behavior via return values and latches, not logs.
org.slf4j.simpleLogger.defaultLogLevel=error
